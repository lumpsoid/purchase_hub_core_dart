name: purchase_hub_core
description: >
  Store-agnostic purchase & subscription abstraction.
  Implement PurchaseAdapter to plug in any store backend.
version: 0.1.1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.17.0

dev_dependencies:
  test: ^1.31.0
  very_good_analysis: ^10.2.0
